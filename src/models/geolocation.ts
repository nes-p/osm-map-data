export interface Coordinates {
  left: number;
  bottom: number;
  right: number;
  top: number;
}

export interface ValidationState {
  top: boolean;
  left: boolean;
  right: boolean;
  bottom: boolean;
}
